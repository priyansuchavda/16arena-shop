// Service layer for Cart feature.
import { type CartItem } from "../types/cart.types";

export async function fetchCartItems(): Promise<CartItem[]> {
  // Skeleton method - return empty cart for now
  return [];
}
